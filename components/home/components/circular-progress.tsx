import { View, StyleSheet } from "react-native";
import React from "react";

const CircularProgress = () => {
  return <View style={styles.circularProgress}></View>;
};

export default CircularProgress;

const styles = StyleSheet.create({
  circularProgress: {},
});
